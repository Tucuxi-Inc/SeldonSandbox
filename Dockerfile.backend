FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src/ ./src/
COPY tests/ ./tests/

RUN pip install --no-cache-dir -e ".[api,dev]"

EXPOSE 8006

CMD ["uvicorn", "seldon.api.app:app", "--host", "0.0.0.0", "--port", "8006"]
